# failog
# .devcontainer (optional)
This repo runs a Streamlit app.

Run:
- pip install -r requirements.txt
- streamlit run app.py

Note:
- planner.db is created automatically.
- fonts/NanumGothic-Regular.ttf can be committed to avoid PDF Korean font issues.
